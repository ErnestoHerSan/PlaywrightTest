const persona= new Persona("Alice",20);
persona.mostrarDetalles;

//una simple funcion
function restart(a:number,b:number):number{
    return a-b;
}

//funcion flecha basica
const suma = (a:number,b:number): number => {
    return a + b;
}

//funcion flecha basica dos
const sumaDos = (a:number,b:number) => {
    a + b;
}